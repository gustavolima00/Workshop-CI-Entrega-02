version: '3'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3030:3000'
    networks:
      - network_app
    image: gustavolim00/trabalho-individual2-frontend

networks:
  network_app:
